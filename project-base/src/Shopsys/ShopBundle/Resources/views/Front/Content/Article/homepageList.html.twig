{% if articles is not empty %}
    <h2>{{ 'Articles'|trans }}</h2>
    <ul class="list-articles">
        {% for article in articles %}
            <li class="list-articles__item">
                <a href="{{ url('front_article_detail', { id: article.id }) }}">
                    <h3>
                        {{ article.name }}
                    </h3>
                    <p>
                        {{ article.text|raw }}
                    </p>
                </a>
            </li>
        {% endfor %}
    </ul>
{% endif %}