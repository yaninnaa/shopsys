{% extends '@ShopsysShop/Admin/Content/Payment/detail.html.twig' %}

{% block title %}- {{ 'Editing payment'|trans }} - {{ paymentDetail.payment.name }}{% endblock %}
{% block h1 %}{{ 'Editing payment'|trans }} - {{ paymentDetail.payment.name }}{% endblock %}

{% block payment_base_prices %}
    <div id="transport_prices" class="box-product-prices">
        <dl class="form-line form-line--visible">
            <dt></dt>
            <dd>
                <div class="box-product-prices__input">
                    <strong>
                        {{ inputPriceLabel() }}
                    </strong>
                </div>
                <div class="box-product-prices__error"></div>
                <div class="box-product-prices__price">
                    <strong>
                        {{ 'Price excluding VAT'|trans }}
                    </strong>
                </div>
                <div class="box-product-prices__price">
                    <strong>
                        {{ 'Price including VAT'|trans }}
                    </strong>
                </div>
            </dd>
        </dl>
        {% for currencyId, currency in currencies %}
            <dl class="form-line">
                <dt>
                    {{ form_label(form.pricesByCurrencyId[currencyId], 'Base price in %currencyCode%'|trans({'%currencyCode%': currency.code}), { isSimple: true } ) }}
                </dt>
                <dd>
                    <div class="box-product-prices__input">
                        {{ form_widget(form.pricesByCurrencyId[currencyId], { symbolAfterInput: currencySymbolByCurrencyId(currencyId) }) }}
                    </div>
                    <div class="box-product-prices__error">
                        {{ form_errors(form.pricesByCurrencyId[currencyId]) }}
                    </div>
                    <div class="box-product-prices__price">
                        {{ paymentDetail.basePricesByCurrencyId[currencyId].priceWithoutVat|priceWithCurrencyByCurrencyId(currencyId) }}
                    </div>
                    <div class="box-product-prices__price">
                        <strong>
                            {{ paymentDetail.basePricesByCurrencyId[currencyId].priceWithVat|priceWithCurrencyByCurrencyId(currencyId) }}
                        </strong>
                    </div>
                </dd>
            </dl>
        {% endfor %}
    </div>
{% endblock %}

{% block image %}
    <dl class="form-line">
        <dd>
            {{ image(paymentDetail.payment, { size: 'original', height: 100 }) }}
        </dd>
    </dl>
{% endblock %}
